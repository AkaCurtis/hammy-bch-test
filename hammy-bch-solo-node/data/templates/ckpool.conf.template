{
  "btcd": [
    {
      "url": "${BCH_RPC_HOST}:${BCH_RPC_PORT}",
      "auth": "${RPC_USER}",
      "pass": "${RPC_PASSWORD}",
      "notify": true
    }
  ],
  "btcaddress": "${PAYOUT_ADDRESS}",
  "btcsig": "/mined by BCH Solo Node on Umbrel/",
  "mindiff": 1,
  "startdiff": 16,
  "maxdiff": 0,
  "logdir": "/www",
  "webdir": "/www/pool",
  "userdir": "/www/users",
  "zmqblock": "tcp://${BCH_RPC_HOST}:${BCH_ZMQ_HASHBLOCK_PORT}"
}
